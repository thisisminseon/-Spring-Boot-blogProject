<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.blog.mapper.UserMapper">

    <!-- 사용자명으로 조회 -->
    <select id="findByUsername"
            parameterType="java.lang.String"
            resultType="com.blog.domain.User">
        SELECT id, username, password, email, role, enabled
        FROM studentuser
        WHERE username = #{username}
    </select>

    <!-- 사용자 추가 -->
    <insert id="insertUser"
            parameterType="com.blog.domain.User"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO studentuser (username, password, email, role, enabled)
        VALUES (#{username}, #{password}, #{email}, #{role}, #{enabled})
    </insert>

    <!-- 사용자명 존재 확인 -->
    <select id="existsByUsername"
            parameterType="java.lang.String"
            resultType="int">
        SELECT COUNT(*)
        FROM studentuser
        WHERE username = #{username}
    </select>

</mapper>